apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth
  type: ClusterIP
  ports: 
    - protocol: TCP
      port: 80      # outside facing port
      targetPort: 80     # insdie of container where the request is forwarded